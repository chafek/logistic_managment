{% extends 'template.html.twig' %}

{% block title %}<title>Gestion des chariots</title>{% endblock %}
{% block pageTitle %}
    <h2 class="text-center m-3 text-success">Gerer les chariots</h2>
    <div class="text-end">
     <a type="btn" href="{{path('show_cart')}}" class="btn btn-outline-primary" >Retour à la liste</a>  
     </div>
{% endblock %}

{% block body %}

    <div class="container-fluid p-0 " >

     {# {{form_start(form) }} 
     <div  >
        {{form_errors(form)}}
    </div> #}
        {# <div class="form-group row">
            <div class="col-xs-2">
                <div >{{form_row(form.)}}</div>
            </div>
        </div> #}
        {# <div class="row col-xs-2">
            <div >{{form_row(form.end_hour)}}</div>
        </div> #}
        {# {{form_end(form)}}  #}
        {{form(form)}}
      
        {% endblock %}