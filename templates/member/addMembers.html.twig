{% extends 'template.html.twig' %}

{% block title %}<title>Gestion des Collaborateurs</title>{% endblock %}
{% block pageTitle %}
    <h2 class='text-center m-3 text-primary'>liste des collaborateurs</h2>
    <div class="text-end">
     <a type="btn" href="{{path('show_member')}}" class="btn btn-outline-primary" >Retour à la liste</a>  
     </div>
{% endblock %}

{% block body %}

<div class="container p-0 " >
     {{form_start(form) }}
     <div  >
        {{form_errors(form)}}
    </div>
        <div class="form-group row">
            <div class="col-xs-2">
                <div >{{form_row(form.firstName)}}</div>
                <div >{{form_row(form.lastName)}}</div>
            </div>
        
        {{form_rest(form)}}
        {{form_end(form)}}

</div>

{% endblock %}
